Options -MultiViews -Indexes
RewriteEngine On

# If accessed directly under /public/, rewrite to root index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ ../index.php [L,QSA]
